services:
  ripen:
    image: ghcr.io/ayato-labs/ripen:latest
    ports:
      - "8377:8377"
    volumes:
      - ripen_data:/data
    environment:
      - RIPEN_HOME=/data
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - LOG_LEVEL=INFO
    restart: unless-stopped

volumes:
  ripen_data:
